import DegreeBuildingWindow from "./DegreeBuilding/DegreeBuildingWindow"

const AdvisorView = () => {

    // This exists to be expanded in the future
    return (
        <>
            <DegreeBuildingWindow></DegreeBuildingWindow>
        </>
    )
}

export default AdvisorView